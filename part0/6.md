```mermaid
sequenceDiagram
    participant browser
    participant server

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    server-->>server: get new_note_spa 
    deactivate server

    note right of browser: browser add the new note spa to the list
```